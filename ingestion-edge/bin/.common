#!/usr/bin/env bash

cd "$(dirname "$0")"/..

LINT_ARGS=( --black --docstyle --flake8 --mypy-ignore-missing-imports )

VENV_DIR="$PWD/venv/$(uname)"

# use venv unless disabled
! ${VENV:-true} || PATH="$VENV_DIR/bin:$PATH"
